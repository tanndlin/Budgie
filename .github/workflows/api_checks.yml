# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: API Checks
on:
  push:
    paths:
      - 'web/functions/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd web/functions && npm ci && npm run build
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd web/functions && npm ci && npm run lint
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd web/functions && npm ci && npm run test
